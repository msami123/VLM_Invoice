# 🧾 استخراج بيانات الفواتير

تطبيق بسيط لاستخراج البيانات من الفواتير باستخدام Google Gemini AI

## 📁 الملفات

```
├── app.py                  # التطبيق الرئيسي
├── image_processor.py      # معالجة الصور
├── prompts.py             # نماذج AI
├── requirements.txt       # المكتبات
├── .env.example           # مثال لملف الإعدادات
└── .gitignore            # ملفات Git المتجاهلة
```

## 🚀 التشغيل

### 1. التثبيت
```bash
pip install -r requirements.txt
```

### 2. إعداد API Key (مهم!)

**أنشئ ملف `.env` في نفس المجلد:**
```bash
GEMINI_API_KEY=your-api-key-here
```

احصل على المفتاح من: https://makersuite.google.com/app/apikey

**ملاحظة مهمة:** ملف `.env` لن يُرفع على GitHub (محمي في .gitignore)

### 3. التشغيل
```bash
streamlit run app.py
```

## 🔒 الأمان

- ✅ API Key مخفي في ملف `.env`
- ✅ ملف `.env` في `.gitignore` (لن يُرفع على GitHub)
- ✅ آمن للاستخدام والنشر

## 🌍 اللغات

- 🇸🇦 العربية
- 🇬🇧 English

اختر اللغة من القائمة المنسدلة في التطبيق

## ✨ المميزات

- استخراج ذكي للبيانات
- استخراج طريقة الدفع ورقم البطاقة
- دعم لغتين
- معالجة الصور
- تصدير JSON
- سريع ورخيص

---

Made with ❤️ using Streamlit & Gemini AI
